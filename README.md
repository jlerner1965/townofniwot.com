# TownofNiwot.com concept

A responsive, static civic-information website concept. Open `index.html` directly or serve the directory with any static HTTP server.

```bash
python3 -m http.server 4173
```

The prototype deliberately marks unverified dynamic content and photography as placeholders. External election and perspective links identify their source role.
